server:
  port: 9999
minio:
  # Minio server addr
  host: http://127.0.0.1:9000
  # MINIO_ACCESS_KEY
  access-key: test
  # MINIO_SECRET_KEY
  secret-key: test
  # default bucket
  default-bucket: test
